# 🔰 คู่มือสำหรับน้องใหม่: ระบบ CI/CD และการติดตั้ง (Onboarding Guide)

ยินดีต้อนรับสู่โปรเจกต์ครับ! คู่มือนี้จัดทำขึ้นเพื่อให้คุณเข้าใจระบบการทำงานอัตโนมัติ (Automated Pipeline) ของเรา และสามารถเริ่มงานได้ทันที

---

## 1. ภาพรวมระบบ (The Big Picture)

เราใช้ **GitHub Actions** เป็นตัวควบคุมเครื่องจักร (Pipeline) โดยมีลำดับงานดังนี้:

1. **CI (Test)**: ตรวจสอบคุณภาพโค้ดบน Node.js หลายเวอร์ชัน
2. **CD (Deploy)**: นำโค้ดที่ผ่านการทดสอบแล้วไปรันบน **Google Cloud Run** (ระบบ Serverless) โดยอัตโนมัติ
3. **Notify**: รายงานผลผ่าน **Discord** ทุกครั้งที่เริ่มและจบงาน

---

## 2. สิ่งที่ต้องมีในเครื่อง (Prerequisites)

ก่อนเริ่มงานแนวนี้ คุณควรติดตั้ง:

- **Node.js**: แนะนำเวอร์ชัน 20 หรือ 22
- **Docker Desktop**: สำหรับการจำลอง Container (ถ้าต้องการรัน Docker ในเครื่อง)
- **Git**: สำหรับการส่งโค้ด

---

## 3. ขั้นตอนการตั้งค่าสำหรับโปรเจกต์ใหม่ (First-time Setup)

หากคุณต้องเริ่มโปรเจกต์ใหม่ด้วยตัวเอง หรือยกระดับโปรเจกต์เดิมให้เป็น CI/CD ให้ทำตามขั้นตอน "กุญแจ 3 ดอก" นี้ครับ:

### 🔑 ดอกที่ 1: เตรียม Dockerfile

กุญแจสำคัญคือไฟล์ `Dockerfile` ใน root directory มันคือสมุดสั่งการว่า "จะให้จัดเตรียม Server อย่างไร" (เช่น ลง Node ตัวไหน, ก๊อปไฟล์ไหนบ้าง)

### 🔑 ดอกที่ 2: เตรียม Google Cloud (GCP)

1. **สร้างที่เก็บ (Artifact Registry):** เข้า GCP Console ไปที่ Artifact Registry สร้างที่เก็บแบบ Docker ในโซน `asia-southeast1`
2. **สร้างไอดี (Service Account):** สร้างไอดีใหม่ใน IAM ให้สิทธิ์ `Cloud Run Admin`, `Storage Admin`, และ `Artifact Registry Writer`
3. **สร้างกุญแจ (JSON Key):** เข้าไปที่ไอดีที่สร้าง แล้วกดบันทึก Key แบบ JSON ลงเครื่อง (ไฟล์นี้สำคัญมาก ห้ามหาย!)

### 🔑 ดอกที่ 3: ตั้งค่าใน GitHub

ไปที่ `Settings` -> `Secrets and variables` -> `Actions` แล้วเพิ่ม 3 ค่านี้:

- `GCP_PROJECT_ID`: ไอดีของโปรเจกต์ใน Google
- `GCP_SA_KEY`: เนื้อหาในไฟล์ JSON ที่โหลดมา (Copy มาทั้งไฟล์)
- `DISCORD_WEBHOOK`: ลิงก์ช่องส่งข้อความใน Discord

---

## 4. วิธีการทำงานในแต่ละวัน (Daily Workflow)

ไม่ต้องกดอะไรใน Google Cloud หรือ GitHub เลยครับ! เพียงแค่:

1. แก้ไขโค้ดใน VS Code
2. รัน Test ในเครื่องก่อนเพื่อความมั่นใจ (`npm test`)
3. ส่งโค้ดขึ้น GitHub:
   ```bash
   git add .
   git commit -m "เขียนอธิบายว่าแก้ตรงไหน"
   git push
   ```
4. รอรับแจ้งเตือนใน Discord และเช็คหน้าเว็บจริงจากลิงก์ที่ส่งมาครับ

---

## 5. ข้อควรระวัง (Do's and Don'ts)

- ✅ **Do:** เขียน Test เสมอ เพราะถ้า Test พัง ระบบจะไม่ยอม Deploy ให้ (ช่วยป้องกันเว็บล่ม)
- ❌ **Don't:** อย่าเผลอ `git push` ไฟล์ `.json` ที่เป็นกุญแจเด็ดขาด (โปรเจกต์นี้เราแอดไว้ใน `.gitignore` แล้ว แต่ต้องระวังเสมอ)
- ✅ **Do:** ลบไฟล์ `.json` ออกจากเครื่องทันทีหลังจากใส่ใน GitHub Secrets เรียบร้อยแล้ว

---

_หากติดขัดตรงไหน หรือเมนูใน Google Cloud เปลี่ยนหน้าตาไป สอบถามพี่ๆ ในทีมได้เลยนะครับ!_ ✌️
